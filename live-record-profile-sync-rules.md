# 直播记录回写用户档案规则

当用户上传某场或某天直播记录，并点击“解析并记录”后，系统按昵称匹配已有用户档案。若昵称已存在，则更新原档案；若昵称不存在，则自动生成一条“直播记录生成”的待完善档案。

## 新增用户统计

- “本月新增”只看用户档案里的 `firstInteraction`。
- 若 `firstInteraction` 属于本月，则计入本月新增。
- 若 `firstInteraction` 为空或不属于本月，则不计入本月新增。
- 用户新增档案、用户编辑并保存档案后，顶部统计数据会根据最新档案数据刷新。

## 回写字段

- `amount`：累计消费金额，按“原累计消费金额 + 本次直播记录识别到的单次/本场消费金额”更新。
- `supportedCount`：本月支持次数，由本月互动记录中 `supported=true` 或 `spendAmount > 0` 的次数自动计算。
- `latestSingleSpendAmount`：最近单次消费，更新为本次直播记录识别到的消费金额。
- `highSingleSpendCount`：单笔大于 1000 次数，若本次消费金额 `> 1000`，则在原次数基础上加 1。
- `maxSingleSpendAmount`：单笔最高消费，取原最高单笔与本次消费金额的最大值。
- `firstInteraction`：首次互动时间，已有则保留；没有则使用本次直播记录日期。
- `lastInteraction`：最近互动时间，更新为本次直播记录日期。
- `tier`：直播记录属于系统更新，会按当前分层规则重新计算。

## 时间精度

- 用户档案中的首次互动时间、最近互动时间只记录到日期。
- 互动时间线也只展示日期，不展示小时和分钟。

## 生日规则

- 生日不是必填项。
- 未填写生日时，页面显示“未知”。
- 只有填写生日后，系统才会自动生成星座标签。
