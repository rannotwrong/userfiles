# 直播记录图片识别规则

这份规则用于约束“直播截图上传后自动解读”的字段提取、字段换算和评分逻辑。核心原则是：可见数据直接提取，缺失数据不编造；热度、人气、观看人数只能辅助判断直播表现，不能反推收入。

## 字段来源

| 字段 | 优先识别来源 | 缺失时处理 |
|---|---|---|
| 日期 | 截图日期、直播后台日期、用户补充文字 | 使用当天日期，并提示用户检查 |
| 总收入 | 本场收入、礼物收益、流水、音浪/钻石折算后的明确金额 | 填 `0`，不根据热度推算 |
| 送礼人数 | 送礼用户数、支持人数、贡献用户数 | 填 `0` |
| 新用户送礼人数 | 新增送礼用户、新支持用户、新关注后支持用户 | 填 `0` |
| 最高价值礼物 | 最高礼物、单个最高价值礼物、榜首礼物 | 留空 |
| 评分 | 收入、送礼人数、新用户送礼人数、互动热度综合判断 | 缺少有效信息时填 `0` |
| 识别文字 | 截图中能确认的直播数据和用户互动摘要 | 保留模型原始摘要 |

## 收入规则

收入只接受截图中的明确数值，不能根据观看人数、在线人数、热度、人气值、点赞数或评论数推算。

可识别为收入的表达包括：

- `本场收入`
- `总收入`
- `礼物收益`
- `直播收益`
- `流水`
- `音浪`
- `钻石`
- `礼物价值`

如果截图只有“热度 3.2 万”“观看 1.8 万”“点赞 12 万”，但没有收入、流水、礼物价值或平台可折算单位，收入必须填 `0`。

## 热度规则

热度用于判断直播表现，不直接进入收入字段。热度相关指标包括：

- 观看人数
- 在线峰值
- 点赞数
- 评论数
- 分享数
- 新增关注
- 直播间热度值
- 停留时长

热度高但收入低，说明曝光不错但支持转化偏弱；收入高但热度低，说明核心用户支持强，但泛流量不足。

## 热度与收入关系

| 场景 | 判断 | 记录建议 |
|---|---|---|
| 高热度、高收入 | 直播内容和用户支持都较强 | 记录有效话术、主题、礼物触发点 |
| 高热度、低收入 | 有曝光但支持转化不足 | 记录互动强度、引导时机、用户停留原因 |
| 低热度、高收入 | 少数核心用户支持明显 | 重点记录核心用户昵称、礼物类型、互动背景 |
| 低热度、低收入 | 整体表现偏弱 | 记录内容主题、开播时段、断点原因 |

不要把“热度高”写成“收入高”，也不要把“收入低”直接归因为用户价值低。直播间常见情况是人气和支持不同步，需要分别记录。

## 评分规则

评分是 0 到 100 的直播表现分，优先用于趋势观察，不作为用户价值的唯一判断。

建议基础权重：

| 维度 | 权重 | 说明 |
|---|---:|---|
| 收入表现 | 40% | 看本场收入、礼物价值、核心用户支持强度 |
| 支持人数 | 25% | 看送礼人数和新用户送礼人数 |
| 热度表现 | 20% | 看观看、在线、点赞、评论、分享等指标 |
| 互动质量 | 15% | 看聊天密度、有效互动、情绪反馈、越界风险 |

评分不要求模型精确计算。截图信息完整时可以给出估算；截图信息不足时填 `0` 或保守低分，并在识别文字中说明缺少哪些依据。

## 分层提示

识别结果只生成直播记录，不自动决定用户分层。用户分层应结合长期数据，包括累计支持、最高单笔支持、互动稳定性、边界感、回应意愿和近期开播表现。

可作为档案备注的信号：

- 多次出现且稳定互动：偏向“稳定陪伴”
- 单场高额支持：记录为“高额支持”候选，不直接升级分层
- 只在高热度场次出现：记录为“活动型用户”或“观望型用户”
- 提出线下见面、吃饭、私下联系：记录边界风险
- 情绪安慰、鼓励、陪伴型话术明显：记录为“情绪支持”

## 模型输出要求

豆包识别应返回严格 JSON，不输出 Markdown：

```json
{
  "date": "YYYY-MM-DD",
  "revenue": 0,
  "giftUsers": 0,
  "newGiftUsers": 0,
  "topGift": "",
  "score": 0,
  "userText": "",
  "confidence": 0.0
}
```

字段无法确认时使用 `0` 或空字符串，不要根据经验补全。`userText` 可以保留对截图内容的自然语言摘要，但需要区分“截图明确显示”和“可能推测”。

## 人工复核点

每次图片识别后，页面应允许用户手动修改结构化字段。建议重点检查：

- 日期是否对应本场直播，而不是截图保存日期。
- 收入单位是否正确，特别是音浪、钻石、礼物价值和人民币之间的区别。
- 送礼人数和观看人数是否被混淆。
- 新用户送礼人数是否和新增关注人数混淆。
- 最高价值礼物是否被截断或识别成用户昵称。
- 评分是否因为热度高被抬得过高。
